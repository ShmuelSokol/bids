# DIBS test plan — 2026-04-28

**Goal:** Validate the k07 cursor-fix patch for SQL writeback, and stand up the Sally REST worker on a whitelisted box. Five tests, in order. Each builds on the previous — stop if one fails and tag me.

---

## Test 1 — Daemon health check (1 minute)

**Goal:** Confirm the recurring daemon is still alive on GLOVE before we do anything else. Yesterday it stopped at 3pm without warning.

**Steps:**
1. Open `https://dibs-gov-production.up.railway.app/settings/lamlinks-writeback`.
2. Look at the **Worker Health** card.

**Success:** Green. Says "Worker ONLINE — last heartbeat under 60s ago" and "Worker on `GLOVE`".

**Failure:** Red, "Worker OFFLINE". On GLOVE (this machine) open PowerShell and run:
```
schtasks /end /tn "DIBS - Recurring Daemon"
schtasks /run /tn "DIBS - Recurring Daemon"
```
Refresh the dashboard. Wait up to 90 seconds for the next heartbeat.

---

## Test 2 — NYEVRVSQL001 whitelist verification (3 minutes)

**Goal:** Find out whether the LL SQL Server box can also reach api.lamlinks.com. If yes, that's our preferred REST worker host (single deployment alongside the existing SQL writeback worker). If no, we deploy on NYEVRVTC001 instead.

**Steps:**
1. RDP into NYEVRVSQL001.
2. Open Notepad: `notepad C:\Windows\Temp\sally-test.ps1`. Click Yes to create. Paste the text below verbatim, save (Ctrl+S), close Notepad.
3. In a regular PowerShell window: `powershell -File C:\Windows\Temp\sally-test.ps1`
4. Open the result: `notepad C:\Windows\Temp\sally-response.xml`

**Notepad contents:**
```
& "G:\PROGRAMS\LAMLINKS\Control\Lamlinkp\LLPservr\code\curl.exe" --digest --data "wait=15&function=are_you_listening&data=%3CRequest%3E%3Clis_function%3Eare_you_listening%3C%2Flis_function%3E%3Ce_code%3E0AG09%3C%2Fe_code%3E%3Creq_data%3E%3C%2Freq_data%3E%3C%2FRequest%3E" -u"ajoseph@everreadygroup.com#7Lx6La4QIpESAgNhfJmSPhWn0Yk:6i^,j5F29jQxCF" -c C:\Windows\Temp\ll-jar.txt "http://api.lamlinks.com/api/llsm/create" -o C:\Windows\Temp\sally-response.xml
```

**Why Notepad first:** RDP autocorrects `--` to em-dash (`–`) on paste, which silently breaks curl. Notepad doesn't autocorrect.

**Success:** XML response containing `<completion_code>1</completion_code>` (or any well-formed `<api_req>` envelope, even one with "Function Not Specified"). NYEVRVSQL001 is whitelisted → use it as REST worker host.

**Failure:** A 401 page or empty file. NYEVRVSQL001 is not whitelisted → fall back to NYEVRVTC001.

Tell me the result and we'll pick the host together.

---

## Test 3 — k07 cursor-fix patch (~5 minutes once a sol is queued)

**Goal:** Validate that yesterday's k07 patch silences the VFP cursor errors Abe was seeing.

**Pre-conditions:**
- Test 1 passed (daemon is up).
- `lamlinks_writeback_enabled` is `true` (already done yesterday).
- Abe has at least one staged envelope in LL (his normal daily save flow).

**Steps:**
1. In DIBS, find one Quoted bid you're comfortable using as a test. (One real bid — not a special test sol. The whole point is to test under production conditions.)
2. Click **Submit** on it.
3. Watch `/ops/dibs-pipeline` — the queue row should flip to `processed` within 30 seconds.
4. Have Abe reopen the bid form in LL.
5. **Watch for cursor errors** (`9999806` or `9999607`).

**Success:** Abe reopens the form, sees our line, no cursor error. The k07 patch worked.

**Failure (cursor error appears):** k07 wasn't enough. The bid still went to DLA — confirm via DLA ack email later. Tell me; we'll dig into what else LL does on Post that we're missing.

**Hard-failure (bid doesn't reach DLA):** Don't expect this — SQL writeback has been reliable. But if it happens, flip the flag back: `npx tsx scripts/_flip-writeback-flag.ts` (edit script first to set `value: 'false'`).

---

## Test 4 — REST worker heartbeat (10-15 minutes — only if Test 2 succeeded)

**Goal:** Get the new Sally REST worker running on the whitelisted box and prove the queue pipeline works end-to-end with a no-risk heartbeat call.

**Steps:**

### 4a. On the chosen box (NYEVRVSQL001 if Test 2 passed, else NYEVRVTC001):

1. Check Node:
   ```
   node --version
   ```
   If not installed: download Node 20 LTS from `https://nodejs.org/en/download/prebuilt-installer`, run installer, accept defaults.

2. Get the repo:
   ```
   cd C:\tmp
   git clone https://github.com/ShmuelSokol/bids dibs-init\dibs
   cd dibs-init\dibs
   npm install
   ```
   (If the box already has the repo, just `git pull` instead.)

3. Copy `.env` from GLOVE to this box (same folder structure, same vars). At minimum need: `LL_SALLY_LOGIN`, `LL_API_KEY`, `LL_API_SECRET`, `LL_E_CODE`, `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`.

4. Start the worker:
   ```
   npx tsx scripts/ll-rest-worker.ts
   ```
   Leave the window open. Should print: `ll-rest-worker starting on <hostname>, polling every 30s`.

### 4b. From any machine (GLOVE is fine), enqueue a heartbeat and watch it complete:
```
npx tsx scripts/_smoke-rest-queue.ts
```
That inserts a row, reads it, deletes it. Replace the test logic with a heartbeat:
```
# Check most recent rows in the queue
npx tsx scripts/_check-worker-state.ts
```

**Success:** A row in `lamlinks_rest_queue` flips from `pending` → `running` → `done` with `completion_code=1` and `response_xml` containing a real envelope.

**Failure (stays pending):** Worker isn't picking up rows. Check the worker's terminal — should be polling every 30s. If silent, restart it.

**Failure (flips to error):** Read `error_message` field. If "401" → creds didn't make it onto the box (wrong .env). If curl spawn errors → wrong path to `curl.exe`.

---

## Test 5 — `put_client_quote` ACL check (5 minutes — only if Test 4 succeeded)

**Goal:** Find out whether ERG is licensed for the `put_client_quote` REST function. This is the open question — same creds got 200 on `get_sent_quotes_by_timeframe` but 403 on `get_quotes_by_timeframe` yesterday.

**Steps:**
1. From any machine, enqueue a `put_client_quote` row with a deliberately incomplete payload (so we don't accidentally submit a real bid):
   ```typescript
   // In a fresh tsx script, or extend _smoke-rest-queue.ts:
   await sb.from("lamlinks_rest_queue").insert({
     lis_function: "put_client_quote",
     e_code: "0AG09",
     req_data_xml: "<test/>",  // intentionally bad — we want to see auth, not transmit
     wait_seconds: 15,
     enqueued_by: "acl-test",
     related_kind: "acl_probe",
   });
   ```
2. Watch the row.

**Success criteria — what each outcome means:**
- `completion_code=1` and `response_xml` mentions "invalid" or schema error → **AUTHORIZED**. ERG can write bids via REST. We can plan migration from SQL.
- `response_xml` contains "API Access Forbidden - 84662" → **NOT AUTHORIZED**. SQL writeback stays the path. Tell Yosef; he'll need to ask LL to add the function to ERG's plan.
- 401 / digest failure → environment problem, not ACL — debug `.env` on the worker box.

---

## Don't do today

- Don't wire DIBS API routes to call `put_client_quote` for real bids. Even if Test 5 passes, we still want a few low-stakes test bids before flipping the production path.
- Don't disable SQL writeback. SQL is the proven path; REST is an experiment until it transmits 5+ real bids cleanly.
- Don't deploy the worker as a Windows service yet. Run it interactively today so we can watch its console output. Service-mode deployment is a follow-up after the heartbeat works.

## Reporting back

After each test, just text me the outcome (Green / Red / unexpected). For Test 5 specifically, paste the `response_xml` content if it's anything other than the three expected outcomes.
